import 'package:flutter/material.dart';

class SettingCrewScreen extends StatelessWidget {
  const SettingCrewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Column(
          children: [
            Text("Setting Crew"),
          ],
        ),
      ),
    );
  }
}
